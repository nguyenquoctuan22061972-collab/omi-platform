# Pipeline (PRD-006 Module C)

Kanban skeleton, standalone. Đồng bộ 6 stage PRD-001 (lead→contacted→qualified→
proposal→won→lost). Deal detail + activity timeline. **Mock state** (`mock/deals.json`),
chỉ integration layer (chưa gọi API thật).

## Chạy
`http://localhost:8000/pipeline/`
