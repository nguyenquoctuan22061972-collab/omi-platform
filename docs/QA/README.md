# QA

Chiến lược & kịch bản kiểm thử (luật #3 — mọi file phải qua QA).

## Nội dung
- Liên kết `../../TESTPLAN.md` (kế hoạch tổng)
- `test-cases/` — ca kiểm thử theo module (bám "Test Case" trong PRD)
- `checklists/` — checklist QA theo loại (docs/code/release)
- Định nghĩa "Done": đủ 4 output + pass QA
