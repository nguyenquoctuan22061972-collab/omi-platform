# QA-001 — CRM Core

**Module:** CRM Core · **PRD:** PRD-001 · **TechSpec:** CRM-Core

## Kết quả
- ✅ **14/14 unit test PASS** (`apps/crm-core/tests/`)
- ✅ **Smoke test 5 endpoint PASS** (POST/GET contacts, messages, conversations, pipeline)
- ✅ Status code đúng: 201 tạo, 200 đọc/cập nhật, 400 stage sai, 404 không thấy

## Map Test Case PRD §10 → test tự động
| Test Case PRD | File | Kết quả |
|---|---|---|
| Trùng số điện thoại phải merge | `test_contacts_merge.py` (4 test) | ✅ |
| Tin nhắn đúng contact | `test_conversations.py` (5 test) | ✅ |
| Pipeline cập nhật | `test_pipeline.py` (4 test) | ✅ |
| KPI thay đổi theo thời gian thực | `test_dashboard_kpi.py` (1 test) | ✅ |

## Lệnh chạy lại
```bash
cd apps/crm-core && python3 -m unittest discover -s tests -p 'test_*.py' -v
```

## Lỗi phát hiện & đã sửa trong QA
- **SQLite cross-thread**: `ThreadingHTTPServer` tạo thread/request gây lỗi
  "SQLite objects created in a thread...". Sửa: dùng `HTTPServer` đơn luồng +
  `check_same_thread=False`. Đã verify bằng smoke test.

## Định nghĩa Done — đạt
File ✅ · Checklist ✅ · Test ✅ · Risk notes ✅ (RISKS.md + PRD §11).
