# API — Hợp đồng API giữa các module

Định nghĩa OpenAPI/JSON schema/endpoint để các module giao tiếp, chống trùng lặp (luật #4).

## Quy ước
- Mỗi service: `api-<service>.md` hoặc `openapi-<service>.yaml`
- Đổi API → cập nhật Tech Spec tương ứng

> _(chưa có hợp đồng API)_
