# PRD-011: Runtime Wiring & Production Activation (Giai đoạn 11)

> **Additive only.** Không đổi PRD-001→010, ADR, capability-map, naming, business logic.
> Nối runtime để go-live thật; kết nối thật đọc env; fail-fast khi thiếu env.

## Phạm vi (A–F)
| Module | Vị trí | Nội dung |
|---|---|---|
| A Runtime Wiring | `libs/runtime/` | env loader fail-fast (AUTH_SECRET/CRM_BASE/N8N_BASE_URL) + unit test |
| B Secrets Validation | `deploy/secrets/validate-env.sh` (--report) + `deploy/runtime-status.md` | PASS/FAIL từng biến, không in secret |
| C Health Gateway | `apps/runtime-health/src/health/gateway.py` + endpoint `/health/gateway` | CRM/n8n reachable, adapter, queue |
| D Dashboard Wiring | `apps/dashboard/operations/health-live.js` | nối widget Health với runtime-health; KPI/revenue giữ nguyên |
| E n8n Runtime Kit | `workflows/runtime/activation-checklist.md` | bật WF001/002/003/050 chỉ khi env hợp lệ |
| F Documentation | `GO_LIVE_DAY.md`, `ROLLBACK_GUIDE.md`, `FIRST_24H_MONITORING.md` | vận hành go-live |

## Ràng buộc
- Fail-fast khi thiếu env; không hardcode giá trị; không in secret.
- Chỉ thêm endpoint/file mới; không sửa endpoint/logger/KPI/business/caller cũ.
- Health gateway checker injectable → CI không network, không API thật.

## QA
libs/runtime + runtime-health gateway + dashboard health-live tests; validate-env --report;
bash -n; node --check; regression; CI xanh.
