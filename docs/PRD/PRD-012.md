# PRD-012: Runtime Observability & Operations (Giai đoạn 12)

> **Additive only.** Không đổi PRD-001→010, ADR, capability-map, naming, business logic.

## Phạm vi (A–F)
| Module | Vị trí | Nội dung |
|---|---|---|
| A Metrics Provider | `libs/metrics/collectors.py` + endpoint `/metrics` | metrics runtime-health/queue/adapters/workflow (Prometheus text, chưa cần Prometheus thật) |
| B Audit Layer | `libs/audit/retention.py` | action login/workflow_trigger/publish/rollback + retention; không log secret |
| C Log Shipping | `libs/logging/shipping.py` | provider file(mặc định)/DB/remote (stub dry-run) |
| D Backup Runtime | `deploy/backup/daily-backup.sh` | daily backup + verify + rotation (không ghi đè script cũ) |
| E Alert Engine | `libs/alerts/` | Telegram + SMTP; chỉ gửi khi *_ENABLED=true; dry-run |
| F Operations Docs | `DISASTER_RECOVERY.md`, `OPS_DAILY_CHECKLIST.md`, `OPS_WEEKLY_CHECKLIST.md` | vận hành |

## Ràng buộc
- Chỉ thêm module/endpoint/file mới; không sửa registry/trail/structured/service cũ.
- Không gọi Prometheus/API/SMTP thật; credential env-only; không log/in secret.

## QA
metrics collectors + /metrics endpoint · audit retention · log shipping · alert engine ·
daily-backup e2e (data giả) · regression; CI xanh.
